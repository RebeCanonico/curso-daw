// uso de puntos y comas
let a, b, c;    //en esta linea estamos declarando tres variables
a = 5; //asignamos en valor 5 a la variable a 
a = 6; //asignamos en valor 5 a la variable b
c = a + b; //asignamos el resultado de sumar a y b, a la variable c

//mientras usemos ; se pueden poner varias declaraiones en una misma linea
a = 5; a = 6; c = a + b;
//esto es igual de valido pero por buenas practicas no se usa

//espacios en blanco
let persona = "Carlos";
let person = "carlos";
//alt + shift + f indenta el codigo por nosotros.+, haciendolo mas legible.
//bloque de codigo
function bloque() {
    let test1 = 0;
    let test2;

    if (condition) {
        let test3;
    }
}